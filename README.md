# Social Media API

## How to Run

To run the application, execute the following command from the root directory:

```bash
go run ./cmd/api
```

This will compile and run the `main` package files located in `cmd/api`.
